#include <stdio.h>
int main() 
{

  int a, b, c, avg; 
  printf("Enter three variables:\n");
  printf("a:");
  scanf("%d", & a); 
  printf("b:");
  scanf("%d", & b); 
  printf("c:");
  scanf("%d", & c); 
  avg = (a + b + c) / 3;

  printf("\n Average of %d,%d and %d is %d", a, b, c, avg);

}
